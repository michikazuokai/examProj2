# **📘 試験データ「完全再ロード」テスト手順（破壊あり / Phase3運用）**

## **目的**
Subject / Exam / Question / 採点系（SEV/SE/Adjust）を **一度クリアして、ゼロから再生成**できることを確認する。  
Phase3（Subjectに fsyear/term を持ち、Examは subject+version）に合わせた運用。

------

## **前提**
- 対象：subjectNo=<span style="color:red"><科目コード></span>, fsyear=2025（必要に応じて読み替え）
- JSON：/Volumes/NBPlan/TTC/examtools/work/answers_<subjectNo>.json
- YAML：exam2/data/studentVersion.yaml（※構造変更あり。下に記載）
- **重要**：手順Bは「破壊あり」です。対象 subjectNo/fsyear を必ず確認してから実行してください。

------

## **変数（読み替え用）**
- <SUBJECT_NO> = <span style="color:red"><科目コード></span>
- <FSYEAR> = 2025

------

# **🧩 YAML構造（学生のA/B割当）**

## **新YAML構造（推奨 / 科目別）**
今回から、学生のA/B割当は「科目別」に持つ。

**構造：**
- 年度 / 1年or2年 / 科目コード / AorB / 学生コード

**例：**
```yaml
2025:
  1:
    2022101:
      A:
        - 25367001
        - 25367002
      B:
        - 25367003
  2:
    2023101:
      A:
        - 26367001
      B:
        - 26367002
```

> 注：`load_student_exam_version` はこの新構造を読むように修正して使用すること。

------

# **✅ 手順B：完全再ロード（破壊あり）**
（問題・割当・採点枠をすべて作り直したいとき）

------

## **0) 事前安全確認（絶対やる）**

### **0-1. 対象 Subject が正しいか確認**
``` 
python manage.py shell -c "from exam2.models import Subject; s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025); print(s.id,s.subjectNo,s.fsyear,s.term,s.nenji,s.name)"
```

### **0-2. 現在の件数（消える量の見積）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,Question,StudentExam,StudentExamVersion,ExamAdjust
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
ex=Exam.objects.filter(subject=s)
print('Exam=',ex.count())
print('Question=',Question.objects.filter(exam__in=ex).count())
print('SEV=',StudentExamVersion.objects.filter(exam__in=ex).count())
print('SE=',StudentExam.objects.filter(exam__in=ex).count())
print('Adjust=',ExamAdjust.objects.filter(exam__in=ex).count())
"
```

------

## **1) clear（破壊）**

### **1-1. clear_subject_data 実行**
``` 
python manage.py clear_subject_data <科目コード>
```

このプログラムで subject 以外のテーブルの関連する科目コードのデータを削除する。

**対象テーブル（想定）**:
- `StudentExam`
- `ExamAdjust`
- （環境により `Exam / Question / SEV` も対象になる場合がある）

### **1-2. clear 後の確認（Phase3の期待値）**
Phase3運用では **Subjectは残し、Exam以下が0** になっているのが正しい。

``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,Question,StudentExam,StudentExamVersion,ExamAdjust
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
ex=Exam.objects.filter(subject=s)
print('Subject=',1)
print('Exam=',ex.count())
print('Question=',Question.objects.filter(exam__in=ex).count())
print('SEV=',StudentExamVersion.objects.filter(exam__in=ex).count())
print('SE=',StudentExam.objects.filter(exam__in=ex).count())
print('Adjust=',ExamAdjust.objects.filter(exam__in=ex).count())
"
```

✅ 期待値：
- Subject=1
- Exam=0
- Question=0
- SEV=0
- SE=0
- Adjust=0

> 補足：Subjectが1件残るのは正常（Phase3の設計どおり）。

------

## **2) load_subject_base（Subject/Exam作成）**

### **2-1. 実行**
``` 
python manage.py load_subject_base <科目コード>
```

### **2-2. 検証（Examバージョンが作られているか）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
print('Subject OK:',s.id,s.term,s.nenji,s.name)
print('Exam versions:',list(Exam.objects.filter(subject=s).order_by('version').values_list('version',flat=True)))
"
```

✅ 期待：['A','B'] などが出る

------

## **3) load_questions（Question再作成）**

### **3-1. 実行（破壊テストなので毎回同じ結果にするため clear 推奨）**
``` 
python manage.py load_questions <科目コード> --fsyear 2025 --clear-existing --fix-qno
```

### **3-2. 検証（version別 Question 件数）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,Question
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
for e in Exam.objects.filter(subject=s).order_by('version'):
    print(e.version, 'Q=', Question.objects.filter(exam=e).count())
"
```

✅ 期待：A/Bで同程度の件数が出る

------

## **4) load_student_exam_version（割当）**

### **4-1. dry-run（必須）**
``` 
python manage.py load_student_exam_version <科目コード> --fsyear 2025 --dry-run
```

### **4-2. 本実行（既存を消して作り直し）**
``` 
python manage.py load_student_exam_version <科目コード> --fsyear 2025 --clear-existing
```

### **4-3. 検証（version別割当数）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,StudentExamVersion
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
for e in Exam.objects.filter(subject=s).order_by('version'):
    print(e.version,'SEV=',StudentExamVersion.objects.filter(exam=e).count())
"
```

------

## **5) load_student_exam（採点行の生成：TF=0,hosei=0）**

### **5-1. dry-run（件数見積）**
``` 
python manage.py load_student_exam <科目コード> --fsyear 2025 --dry-run
```

### **5-2. 本実行**
``` 
python manage.py load_student_exam <科目コード> --fsyear 2025 --batch-size 2000
```

### **5-3. 検証（総件数）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,StudentExam
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
ex=Exam.objects.filter(subject=s)
print('StudentExam total=',StudentExam.objects.filter(exam__in=ex).count())
"
```

------

## **6) load_exam_adjust（補正行の生成：adjust=0）**

### **6-1. dry-run**
``` 
python manage.py load_exam_adjust <科目コード> --fsyear 2025 --dry-run
```

### **6-2. 本実行**
``` 
python manage.py load_exam_adjust <科目コード> --fsyear 2025 --batch-size 2000
```

### **6-3. 検証（総件数）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,ExamAdjust
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
ex=Exam.objects.filter(subject=s)
print('ExamAdjust total=',ExamAdjust.objects.filter(exam__in=ex).count())
"
```

------

# **✅ 最終チェック（UI/API観点）**
- index で subject を選ぶ → 学生一覧が出る
- 学生を開く（採点画面） → タイトルに **version(A/B)** が表示される
- 行一括 ON/OFF → /api/student-exams/bulk_update/ が **500にならない**
- cancel → 元に戻る（bulk_update でDBも戻る）

------

# **✅ 手順C：学生のA/B割当の変更（解答入力前の入替）**
（「一旦データロードが完了」した後、**解答入力前に** A/B の割当が変わったとき）

## **前提（この手順が使える条件）**
- 学生の回答欄やアクセスログがDBに無い前提のため、以下で「痕跡なし」を判定する：
  - StudentExam の **tf=0 かつ hosei=0**
  - ExamAdjust が **0件（または補正値合計が0）**
- 上記が満たせない場合は、入替は行わず、運用判断（または完全再ロード）を検討する。

------

## **C-0) 現状チェック（痕跡なし判定）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,StudentExam,ExamAdjust
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
ex=Exam.objects.filter(subject=s)
print('SE total=',StudentExam.objects.filter(exam__in=ex).count())
print('SE tf!=0 =',StudentExam.objects.filter(exam__in=ex).exclude(tf=0).count())
print('SE hosei!=0 =',StudentExam.objects.filter(exam__in=ex).exclude(hosei=0).count())
print('Adjust total=',ExamAdjust.objects.filter(exam__in=ex).count())
"
```

✅ 期待：
- tf!=0 が 0
- hosei!=0 が 0
- Adjust total が 0（または補正が全て0）

------

## **C-1) YAML を修正（新構造で科目別にA/Bを入替）**
- `exam2/data/studentVersion.yaml` を編集
- 対象は `fsyear -> nenji -> subjectNo -> A/B` ブロック

------

## **C-2) runtimeデータのクリア（StudentExam / ExamAdjust のみ）**
> ここでは Subject/Exam/Question/SEV は消さない。  
> （StudentExam / ExamAdjust は入替後に再生成するため）

### **C-2-1. dry-run（必須）**
``` 
python manage.py clear_subject_runtime_data <科目コード> --fsyear 2025 --dry-run
```

### **C-2-2. 本実行**
``` 
python manage.py clear_subject_runtime_data <科目コード> --fsyear 2025 --execute
```

------

## **C-3) 割当（SEV）の作り直し**
### **C-3-1. dry-run（必須）**
``` 
python manage.py load_student_exam_version <科目コード> --fsyear 2025 --dry-run
```

### **C-3-2. 本実行（既存を消して作り直し）**
``` 
python manage.py load_student_exam_version <科目コード> --fsyear 2025 --clear-existing
```

------

## **C-4) StudentExam の再生成**
### **C-4-1. dry-run**
``` 
python manage.py load_student_exam <科目コード> --fsyear 2025 --dry-run
```

### **C-4-2. 本実行**
``` 
python manage.py load_student_exam <科目コード> --fsyear 2025 --batch-size 2000
```

------

## **C-5) ExamAdjust の再生成**
### **C-5-1. dry-run**
``` 
python manage.py load_exam_adjust <科目コード> --fsyear 2025 --dry-run
```

### **C-5-2. 本実行**
``` 
python manage.py load_exam_adjust <科目コード> --fsyear 2025 --batch-size 2000
```

------

## **C-6) 最終確認（version表示と件数）**
``` 
python manage.py shell -c "
from exam2.models import Subject,Exam,StudentExamVersion,StudentExam,ExamAdjust
s=Subject.objects.get(subjectNo='<科目コード>',fsyear=2025)
for e in Exam.objects.filter(subject=s).order_by('version'):
    print(e.version,'SEV=',StudentExamVersion.objects.filter(exam=e).count())
    print(e.version,'SE =',StudentExam.objects.filter(exam=e).count())
    print(e.version,'Adj=',ExamAdjust.objects.filter(exam=e).count())
"
```

------

# **⚠️ 重要メモ（Phase3）**
- Phase3運用では、**Subjectは残して Exam以下を消す**のが安全。
- 「問題が変わった」場合は、手順B（完全再ロード）を推奨。
- 「割当だけ変わった（解答入力前）」場合は、手順C（runtimeのみクリア→再生成）で対応する。

------

# **🧯 トラブルシュート**

## **YAML に fsyear=2025 のブロックがありません**
原因：YAMLキー型（int/str）の違い、または新構造の階層が不足。

新構造の例（最低限）：
```yaml
2025:
  1:
    2022101:
      A:
        - 25367001
      B:
        - 25367002
```

確認ポイント：
- 2025 が存在するか
- 1/2（nenji）が存在するか
- subjectNo ブロックが存在するか
- その下に A/B があるか

## **YAML に nenji=1 のブロックがありません**
- Subject.nenji が 1/2 と一致しているか確認
- YAML側の nenji キーが文字列になっていないか（"1" など）

## **YAML に subjectNo=XXXX のブロックがありません**
- 新構造では `nenji` の下に `subjectNo` が必須
- subjectNo の文字列/数値扱いが混在していないか（"2022101" など）

## **入替手順Cで削除が止まる（ガードに引っかかる）**
- tf!=0 / hosei!=0 / Adjustが残っている可能性
- すでに採点作業が始まっている疑いがあるため、運用判断すること
  - 原則：入替しない
  - どうしてもやる場合は、完全再ロード（手順B）を検討
